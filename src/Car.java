import java.awt.*;

public class Car {
    int weight;
    Color colour;
    int maxSpeed;

    public Car(int weight, int maxSpeed, Color colour) {
        this.weight = weight;
        this.maxSpeed = maxSpeed;
        this.colour = colour;
    }
}
